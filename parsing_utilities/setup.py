from setuptools import setup, find_packages

setup(
    name='parsing_utilities',
    version='0.1',
    description='Functions to help with processing dates and times for command-line scripts',
    packages=find_packages(),
    python_requires='>=2.7'
)